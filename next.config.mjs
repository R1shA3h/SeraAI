/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        remotePatterns: [
          {
            protocol: 'https',
            hostname: "D:\Siri2.0\youtube-ai-assistant-build\img\img\myphoto.jpg",
          },
        ],
      },
};

export default nextConfig;
